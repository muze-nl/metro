# @muze-nl/metro-trace

Optional tracing helpers for Metro.

```js
import * as trace from '@muze-nl/metro-trace'

trace.add('graph', trace.graph({ view: 'tree' }))
```

The combined `@muze-nl/metro` package exposes the same helpers as `metro.trace.*`.
