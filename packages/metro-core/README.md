# @muze-nl/metro-core

Small Fetch-compatible Metro core: `client`, `Client`, `request`, `response`, `url`, `metroError`, and `deepClone`.

See [`docs/`](docs/) for installation, usage, and reference.
