# @muze-nl/metro

Beginner-friendly combined Metro package. It re-exports the core client, API helpers, generic middleware, tracing helpers, hash-parameter helpers, and `formdata()`. The browser bundle also creates `globalThis.metro`.

See [`docs/`](docs/) for installation, usage, and reference.
